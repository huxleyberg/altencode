import {environment} from '../../environments/environment';

export const api_routes = {
    GET_ALL_CUSTOMERS: environment.api_base_url + '/customers',
    SEARCH: environment.api_base_url + '/vehicles/',
    GET_ALL_VEHICLES: environment.api_base_url + '/vehicles',
    
}